apiVersion: v1
kind: Service
metadata:
  name: {{ include "safeai-dashboard.fullname" . }}
  labels:
    {{- include "safeai-dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "safeai-dashboard.selectorLabels" . | nindent 4 }}
